# Book API Contract

This file is generated from `src/lib.rs` and represents the public symbols the book should treat as current API.

## Public Modules

```text
compression
container
container_v2
error
frame
geojson
hilbert
id
ids
io
lattice
layer
layers
morton
neighbors
path
performance
```

## Public Re-Exports

```text
crate::container_v2::{ContainerWriterV2, HeaderV2, StreamConfig}
crate::error::{Error, Result}
crate::frame::{get_frame, list_frames, register_frame, FrameDescriptor}
crate::geojson::{
crate::hilbert::Hilbert64
crate::id::CellID
crate::ids::{FrameId, Galactic128, Index64, Route64}
crate::lattice::{Lattice, LatticeCoord, Parity, BCC_NEIGHBORS_14}
crate::layers::{
crate::performance::{Backend, BatchIndexBuilder, BatchNeighborCalculator, BatchResult}
crate::performance::{GpuBackend, GpuBatchProcessor}
crate::performance::{ParallelBatchIndexBuilder, ParallelBatchNeighborCalculator}
```

## Layers Re-Exports

```text
bcc_utils::{is_valid_bcc, physical_to_bcc_voxel, snap_to_nearest_bcc}
esdf::ESDFLayer
exploration::{Frontier, FrontierDetectionConfig, InformationGainConfig, Viewpoint}
export::{export_mesh_obj, export_mesh_ply, export_mesh_stl}
measurement::{Measurement, MeasurementType}
mesh::{extract_mesh_from_tsdf, Mesh, MeshStats, Triangle, Vertex}
occupancy::{OccupancyLayer, OccupancyState, OccupancyStats}
occupancy_compressed::{CompressedOccupancyLayer, CompressionMethod, CompressionStats}
occupancy_temporal::{TemporalConfig, TemporalOccupancyLayer, TemporalStats}
tsdf::TSDFLayer
```

## Performance Re-Exports

```text
arch_optimized::{has_bmi2, ArchInfo}
avx512::{batch_neighbors_avx512, has_avx512f, Avx512Info}
batch::{BatchIndexBuilder, BatchNeighborCalculator, BatchResult}
fast_neighbors::{batch_neighbors_auto, neighbors_route64_fast, NeighborStream}
gpu::{GpuBackend, GpuBatchProcessor}
hilbert_batch::{batch_hilbert_decode, batch_hilbert_encode}
memory::{AlignedBatchProcessor, AlignedVec, NumaInfo, CACHE_LINE_SIZE}
morton_batch::{batch_morton_decode, batch_morton_encode}
parallel::{ParallelBatchIndexBuilder, ParallelBatchNeighborCalculator}
simd_batch::{
```

Generated by: `scripts/generate_book_api_contract.sh`
